# Decisions log

A running list of design and configuration calls that shape the experiment, with the reason.

## Phase 0

### 2026-05-19 — Base vs. instruct: `Llama-3.2-1B-Instruct`
Owner-confirmed. The framing ("AI interactions get a sleep cycle") is closer to instruct-style than base completion. Per PRD §8 decision 2, instruct was the recommended call. Going forward all references to "the model" mean `meta-llama/Llama-3.2-1B-Instruct` unless explicitly stated.

### 2026-05-19 — Compute: AMD MI300X via Modal
Owner-confirmed. PRD §5.1 + §8 decision 1. Phase 0 must verify ROCm/PEFT load on a Modal MI300X container before any sweep spend.

### 2026-05-19 — Parallelism: sequential arms, one Modal job at a time
Owner-confirmed. Wall time is small for 1B + LoRA; integrity over speedup avoids races on checkpoint / dataset paths.

### 2026-05-19 — Replay budget is measured in training tokens, not example count
Required by the equal-budget invariant. Token-level matching is the only way to make "same compute" credible when flagged and unflagged examples may differ in length.

### 2026-05-19 — Paraphrased probes are template-based, NOT generated by the model under test
Leak risk. Paraphrasing with the same model that will be evaluated would give that model an unfair advantage during probe generation. Handcrafted templates with slot-fills only.

## Phase 1 (pending)

(Interference parameters, gate result, any backstep trigger will be recorded here.)

## Phase 2 (pending)

(Oversampling ratio r choice, seed list, replay budget B will be recorded here.)
